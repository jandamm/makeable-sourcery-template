{% for t in types.structs.implementing.Makeable %}func make{{ t.name }}{% for v in t.variables %}({% if not forloop.first %}_ {% endif %}{{ v.name }}: {{ v.typeName }}) -> {% endfor %}{{ t.name }} {
    return {% for v in t.variables %}{% if not forloop.first %}{ {{ v.name }} in {% endif %}{% endfor %}{{ t.name }}({% for v in t.variables %}{{ v.name }}: {{ v.name }}{% if not forloop.last %}, {% endif %}{% endfor %}){% for v in t.variables %}{% if not forloop.first %} }{% endif %}{% endfor %}
}
{% if not forloop.last %}

{% endif %}{% endfor %}